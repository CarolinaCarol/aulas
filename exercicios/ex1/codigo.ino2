cons int ledVm = 1;
const int ledVm = 2;

void setup()
{
  pinMode(ledVm, OUTPUT);
  pinMode (ledVm, INPUT);
}
int var;

void loop()
{
  var = digitalRead(1);
  if(var)
    digitalWrite(ledVm, HIGH);
}

void loop()
{
  var = digitalRead(2);
  if (var)
    digitalWrite (ledVm, LOW);
}
